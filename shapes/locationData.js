import PropTypes from 'prop-types'

export default {
  ip: PropTypes.string,
  contryName: PropTypes.string,
  regionName: PropTypes.string,
  city: PropTypes.string,
  zipCode: PropTypes.string,
  lat: PropTypes.number,
  lng: PropTypes.number
}
